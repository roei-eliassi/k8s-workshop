apiVersion: v2
name: wordpress-app
description: Umbrella chart deploying WordPress with a MariaDB backend
type: application
version: 0.1.0
appVersion: "1.0.0"

dependencies:
  - name: wordpress
    version: "0.1.0"
    repository: "file://charts/wordpress"

  - name: mariadb
    version: "0.1.0"
    repository: "file://charts/mariadb"
